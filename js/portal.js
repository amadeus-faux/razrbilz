function enterSite() { const t = document.getElementById("portal"); t.style.transition = "opacity 0.5s ease", t.style.opacity = "0", setTimeout(() => { t.classList.add("hidden"), t.style.opacity = "1", navigate("archive") }, 500) }
